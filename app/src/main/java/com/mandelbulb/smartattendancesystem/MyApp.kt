package com.mandelbulb.smartattendancesystem

import android.app.Application

class MyApp : Application() {
    
    override fun onCreate() {
        super.onCreate()
        // Application initialization
        // WorkManager sync will be set up later when needed
    }
}
